# 📱 MANTIS Mobile - Visual Guide

## 🎨 Screen Flow

```
┌─────────────────────┐
│   Login Screen      │
│                     │
│  [Email Input]      │
│  [Password Input]   │
│  [Sign In Button]   │
│                     │
│  Loading spinner... │
└──────────┬──────────┘
           │
           │ Authenticated
           ▼
┌─────────────────────┐
│  Dashboard Screen   │
│  (Home Tab)         │
│                     │
│  Welcome, Officer   │
│  🟢 Online 📍 GPS   │
│                     │
│  ┌──────┬──────┐    │
│  │  5   │  24  │    │
│  │Today │ Week │    │
│  ├──────┼──────┤    │
│  │  89  │ 342  │    │
│  │Month │Total │    │
│  └──────┴──────┘    │
│                     │
│  📝 Record          │
│  📋 View            │
│                     │
│  📍 Current:        │
│  -25.xxx, 28.xxx    │
└──────────┬──────────┘
           │
           │ Tap "Record"
           ▼
┌─────────────────────┐
│  Record Screen      │
│                     │
│  🟢 Online 🔄 GPS   │
│                     │
│  📍 Current Loc:    │
│  -25.746, 28.188    │
│  Accuracy: ±8m      │
│                     │
│  Vehicle ID *       │
│  [ABC123GP____]     │
│                     │
│  Type *             │
│  [Speeding] [Park]  │
│  [No Seatbelt]...   │
│                     │
│  Notes              │
│  [____________]     │
│  [____________]     │
│                     │
│  [Record Button]    │
└─────────────────────┘
```

---

## 🎨 Component Hierarchy

```
App (_layout.tsx)
├── AuthContext Provider
│   └── Session State
│       ├── User Profile
│       └── Auth Methods
│
├── Login Screen (if not authenticated)
│   └── Sign In Form
│
└── Tab Navigator (if authenticated)
    ├── Dashboard Tab (index.tsx)
    │   ├── Header (name, sign out)
    │   ├── Status Badges (online, GPS)
    │   ├── Stats Grid (4 cards)
    │   ├── Quick Actions (2 buttons)
    │   └── Location Card
    │
    └── Explore Tab (explore.tsx)
        └── To be replaced with list

Modal Routes
└── Record Screen (record.tsx)
    ├── Status Bar
    ├── Location Card
    ├── Form Fields
    │   ├── Vehicle Input
    │   ├── Type Selector
    │   └── Notes Input
    └── Submit Button
```

---

## 🎯 Data Flow Diagram

```
┌─────────────────────────────────────────┐
│          Supabase Backend               │
│                                         │
│  ┌────────┐  ┌──────────┐  ┌────────┐ │
│  │ auth.  │  │infring-  │  │ gps_   │ │
│  │ users  │  │ ements   │  │tracking│ │
│  └────────┘  └──────────┘  └────────┘ │
└────────┬────────────────────────────────┘
         │
         │ API Calls (when online)
         │
┌────────▼────────────────────────────────┐
│       Supabase Client (supabase.ts)     │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  CRUD Helpers:                  │   │
│  │  - auth.signIn()                │   │
│  │  - profiles.get()               │   │
│  │  - infringements.create()       │   │
│  │  - gpsTracking.save()           │   │
│  └─────────────────────────────────┘   │
└────────┬────────────────────────────────┘
         │
         │ Exposed via Context
         │
┌────────▼────────────────────────────────┐
│       AuthContext (AuthContext.tsx)     │
│                                         │
│  State:                                 │
│  - session (user object)                │
│  - profile (officer data)               │
│  - loading (boolean)                    │
│                                         │
│  Methods:                               │
│  - signIn(email, password)              │
│  - signOut()                            │
│  - refreshProfile()                     │
└────────┬────────────────────────────────┘
         │
         │ Used by all screens
         │
┌────────▼────────────────────────────────┐
│           App Screens                   │
│                                         │
│  ┌──────────┐  ┌──────────┐            │
│  │Dashboard │  │  Record  │            │
│  │          │  │          │            │
│  │ useAuth()│  │ useAuth()│            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘

         ┌─────────────────┐
         │  GPS Service    │
         │                 │
         │ - getCurrentLoc │
         │ - startTracking │
         │ - queueOffline  │
         └─────────────────┘

         ┌─────────────────┐
         │  AsyncStorage   │
         │  (Offline Queue)│
         │                 │
         │ - infringements │
         │ - gps_points    │
         │ - profile_cache │
         └─────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────────┐
│  Check Session  │
│  (SecureStore)  │
└────┬────────────┘
     │
     ├─── Has Session ───┐
     │                   │
     │                   ▼
     │           ┌──────────────┐
     │           │  Load Profile│
     │           │  from Cache  │
     │           └──────┬───────┘
     │                  │
     │                  ▼
     │           ┌──────────────┐
     │           │  Show Dash   │
     │           └──────────────┘
     │
     └─── No Session ───┐
                        │
                        ▼
                 ┌──────────────┐
                 │ Show Login   │
                 └──────┬───────┘
                        │
                        ▼
                 ┌──────────────┐
                 │ User Enters  │
                 │ Credentials  │
                 └──────┬───────┘
                        │
                        ▼
                 ┌──────────────┐
                 │ Supabase Auth│
                 └──────┬───────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
   ┌────────┐    ┌──────────┐    ┌─────────┐
   │ Success│    │  Error   │    │ Loading │
   └────┬───┘    └──────────┘    └─────────┘
        │
        ▼
   ┌──────────────┐
   │ Save Session │
   │ (SecureStore)│
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Load Profile │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Navigate to │
   │  Dashboard   │
   └──────────────┘
```

---

## 📍 GPS Tracking Flow

```
┌──────────────┐
│ App Launches │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Check GPS Perms  │
└──────┬───────────┘
       │
       ├─── Granted ───┐
       │               │
       │               ▼
       │        ┌─────────────┐
       │        │Get Current  │
       │        │Location     │
       │        └──────┬──────┘
       │               │
       │               ▼
       │        ┌─────────────┐
       │        │Display on   │
       │        │Dashboard    │
       │        └─────────────┘
       │
       └─── Denied ────┐
                       │
                       ▼
                ┌─────────────┐
                │Show Alert   │
                │"Enable GPS" │
                └──────┬──────┘
                       │
                       ▼
                ┌─────────────┐
                │Request Perms│
                └─────────────┘

Background Tracking:
┌─────────────────┐
│ Start Tracking  │
│ (when signed in)│
└────────┬────────┘
         │
         ▼
┌──────────────────────┐
│ Every 30s or 100m:   │
│  1. Get Location     │
│  2. Check Network    │
│  3. If Online:       │
│     - Save to DB     │
│  4. If Offline:      │
│     - Queue locally  │
└──────────────────────┘
```

---

## 💾 Offline Sync Strategy

```
User Action (Record Infringement)
         │
         ▼
┌──────────────────┐
│ Check Network    │
│ with NetInfo     │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
Online      Offline
    │         │
    ▼         ▼
┌─────────┐ ┌──────────────┐
│ Submit  │ │Save to       │
│ to API  │ │AsyncStorage  │
└────┬────┘ └──────┬───────┘
     │             │
     ▼             ▼
┌─────────┐ ┌──────────────┐
│ Success │ │Queue:        │
│ Message │ │{             │
└─────────┘ │ data,        │
            │ timestamp,   │
            │ offline:true │
            │}             │
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │User notified:│
            │"Saved        │
            │ Offline"     │
            └──────────────┘

Next App Open (or Manual Sync):
         │
         ▼
┌──────────────────┐
│ Check Network    │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
Online      Offline
    │         │
    ▼         │
┌─────────────┤
│Load Queue   │
│from Storage │
└──────┬──────┘
       │
       ▼
┌──────────────┐
│For each item │
│in queue:     │
│ 1. Submit    │
│ 2. If OK:    │
│    Remove    │
│ 3. If Error: │
│    Keep      │
└──────────────┘
```

---

## 🎨 UI States

### Dashboard Stats Card
```
┌─────────────────┐
│                 │ ← Shadow/elevation
│       5         │ ← Big number (32px)
│     Today       │ ← Label (14px gray)
│                 │
└─────────────────┘
Primary (blue bg, white text) for "Today"
White bg, dark text for others
```

### Online/Offline Badge
```
Online:           Offline:
┌────────────┐    ┌────────────┐
│ 🟢 Online  │    │ 🔴 Offline │
└────────────┘    └────────────┘
Green bg          Red bg
```

### GPS Status Badge
```
Active:             Inactive:
┌──────────────┐    ┌──────────────┐
│ 📍 GPS Active│    │ 📍 GPS Off   │
└──────────────┘    └──────────────┘
Green bg            Red bg
```

### Type Selection Chips
```
Unselected:
┌─────────────┐
│  Speeding   │ ← Gray border
│    R500     │ ← Gray text
└─────────────┘

Selected:
┌─────────────┐
│  Speeding   │ ← Blue bg
│    R500     │ ← White text
└─────────────┘
```

---

## 📊 Data Models

### Infringement Record
```typescript
{
  id: string;              // UUID
  vehicle_id: string;      // "ABC123GP"
  infringement_type_id: string;
  officer_id: string;      // From auth user
  latitude: number;        // -25.746
  longitude: number;       // 28.188
  location_accuracy: number; // meters
  notes: string | null;
  status: 'pending' | 'paid' | 'disputed';
  fine_amount: number;     // cents
  issued_at: string;       // ISO timestamp
  created_at: string;
  updated_at: string;
}
```

### GPS Tracking Point
```typescript
{
  id: string;
  officer_id: string;
  latitude: number;
  longitude: number;
  accuracy: number;
  speed: number | null;
  heading: number | null;
  recorded_at: string;
  created_at: string;
}
```

### User Profile
```typescript
{
  id: string;              // Matches auth.users.id
  email: string;
  full_name: string;
  position: string;        // "Traffic Officer"
  badge_number: string;
  department_id: string;
  role: 'officer' | 'admin' | 'supervisor';
  is_active: boolean;
  created_at: string;
  updated_at: string;
}
```

---

## 🎯 Performance Metrics

### Load Times (Target)
- App Launch: < 2s
- Login: < 1s
- Dashboard Load: < 1s
- Record Screen: < 0.5s
- GPS Fix: < 5s
- Form Submit: < 1s

### Offline Support
- Queue capacity: 1000 items
- Sync batch size: 50 items
- Retry attempts: 3
- Retry delay: 5s exponential backoff

---

## 🔧 Development Tools

### Debugging
```bash
# View logs
npx expo start

# Clear cache
npx expo start -c

# Android logs
adb logcat *:S ReactNative:V ReactNativeJS:V

# Check AsyncStorage
# Use React Native Debugger
```

### Testing GPS
```bash
# Android Emulator
# Use Extended Controls > Location
# Or send coordinates via adb:
adb emu geo fix 28.188 -25.746
```

---

## 📱 Screenshots (Mockup)

### Login Screen
```
╔═══════════════════════════╗
║                           ║
║     MANTIS Mobile         ║
║     Traffic Officers      ║
║                           ║
║   ┌─────────────────┐     ║
║   │ Email           │     ║
║   └─────────────────┘     ║
║                           ║
║   ┌─────────────────┐     ║
║   │ Password        │     ║
║   └─────────────────┘     ║
║                           ║
║   ┌─────────────────┐     ║
║   │    Sign In      │     ║
║   └─────────────────┘     ║
║                           ║
╚═══════════════════════════╝
```

### Dashboard
```
╔═══════════════════════════╗
║ Welcome back,             ║
║ Officer John Smith        ║
║                           ║
║ 🟢 Online  📍 GPS Active  ║
║                           ║
║ ┌─────────┬─────────┐     ║
║ │    5    │   24    │     ║
║ │  Today  │ This Wk │     ║
║ ├─────────┼─────────┤     ║
║ │   89    │   342   │     ║
║ │This Mo  │  Total  │     ║
║ └─────────┴─────────┘     ║
║                           ║
║ Quick Actions             ║
║ ┌─────────────────────┐   ║
║ │ 📝 Record Infring.  │→  ║
║ └─────────────────────┘   ║
║ ┌─────────────────────┐   ║
║ │ 📋 View Infring.    │→  ║
║ └─────────────────────┘   ║
║                           ║
║ 📍 Current Location       ║
║ -25.746, 28.188           ║
║ Accuracy: ±8m             ║
╚═══════════════════════════╝
```

### Record Screen
```
╔═══════════════════════════╗
║ ← Back                    ║
║ Record Infringement       ║
║                           ║
║ 🟢 Online   🔄 Refresh    ║
║                           ║
║ 📍 Current Location       ║
║ -25.746, 28.188           ║
║ Accuracy: ±8m             ║
║                           ║
║ Vehicle Registration *    ║
║ ┌─────────────────────┐   ║
║ │ ABC123GP            │   ║
║ └─────────────────────┘   ║
║                           ║
║ Infringement Type *       ║
║ ┌────┬────┬────┬────┐     ║
║ │Spd │Park│Seat│Red │     ║
║ │R500│R300│R200│R700│     ║
║ └────┴────┴────┴────┘     ║
║                           ║
║ Notes                     ║
║ ┌─────────────────────┐   ║
║ │                     │   ║
║ └─────────────────────┘   ║
║                           ║
║ ┌─────────────────────┐   ║
║ │ Record Infringement │   ║
║ └─────────────────────┘   ║
╚═══════════════════════════╝
```

---

*Visual guide for MANTIS Mobile App*  
*Sprint 2 Complete - Ready for Sprint 3*
